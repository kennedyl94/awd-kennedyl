<%= form_for(@task) do |f| %>

<%= params %>
  <% if @task.errors.any? %>
  
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :timespent %><br>
    <%= f.number_field :timespent %>
  </div>
  <div class="field">
    <%= f.label :isDone %><br>
    <%= f.check_box :isDone %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <%= link_to 'add time', new_time_span_path, params:{task_id:params[:id]} %>
  
  
<% end %>
